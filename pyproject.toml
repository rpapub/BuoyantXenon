[project]
name = "buoyantxenon-tools"
version = "0.1.0"
description = "Pinned Python tooling for the REFramework extraction pipeline"
requires-python = ">=3.9"
dependencies = [
    "git-filter-repo==2.47.0",
]
